Client(leo):
Bruk mapbox og kanskje leaflet:
https://www.mapbox.com/ (for å hente kart information)
https://leafletjs.com/ (for å vise mappet på nettsiden)
https://leafletjs.com/reference.html#circle (for å lage sirkel)


Server(kong håkon):

Requests {
    HostGame {
        center: {
            lon: number,
            lat: number,
        },

        raduis: number,
    }

    UpdatePosition {
        positon: {
            lon: number,
            lat: number,
        }

        userid: string
    }
}

Response {
    HostGame {
        success: boolean
    },

    UpdatePosition {
        damage: boolean,
        timeleft: number,
        center: {
            lon: number,
            lat: number,
        },

        radius: number

        players [
        {
            lon: number,
            lat: number,

            userid: string
        }...
        ]
    },


}


Notater:

Simpel matte for kalkulere avstand mellom to lon og lat

lat1 & lon1 er player positon
lat2 & lon2 er centere av sirkelen

(lat1, lat2, lon1,lon2)

const R = 6371e3; // metres
const φ1 = lat1 * Math.PI/180; // φ, λ in radians
const φ2 = lat2 * Math.PI/180;

const Δφ = (lat2-lat1) * Math.PI/180;
const Δλ = (lon2-lon1) * Math.PI/180;

const a = Math.sin(Δφ/2) * Math.sin(Δφ/2) +
          Math.cos(φ1) * Math.cos(φ2) *
          Math.sin(Δλ/2) * Math.sin(Δλ/2);
const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a));

const d = R * c; // in meters

return d